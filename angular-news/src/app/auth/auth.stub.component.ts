import {Component} from '@angular/core';
import {AuthComponent} from './auth.component';

@Component({
  selector: 'auth',
  template: '<div></div>'
})
export class AuthStubComponent extends AuthComponent {
}
